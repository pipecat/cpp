#include <string>
#include <iostream>
#include <unordered_map>
using namespace std;
int main() {
    string a;
    int m, n;
    cin >> m >> n;
    cin >> a;
    for (int i=0; i<n; i++) {
        string tmp;
        cin >> tmp;
        
    }
    unordered_map<string> set;

    return 0;
}